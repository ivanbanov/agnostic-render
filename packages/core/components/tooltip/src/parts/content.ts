/**
 * Content — the painted box that holds the tooltip's children.
 *
 * Variant declaration only. Paint lives in `../shared/styles.ts`.
 */

export type ContentVariants = {
  side: "top" | "bottom" | "left" | "right";
  red: "true" | "false";
};
