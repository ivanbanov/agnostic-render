/**
 * Positioner — zero-size anchor box that hosts the menu content.
 * Variant declaration only. Paint lives in ../shared/styles.ts.
 */

export type PositionerVariants = {
  anchored: "true" | "false";
};
