/**
 * Item — one selectable row in the menu.
 * Variant declaration only. Paint lives in ../shared/styles.ts.
 */

export type ItemVariants = {
  highlighted: "true" | "false";
  disabled: "true" | "false";
};
