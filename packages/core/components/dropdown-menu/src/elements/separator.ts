/**
 * DropdownMenu separator — visual divider between groups of items.
 * Cosmetic-only.
 */

import type { StyleSpec } from "@render-experiment/machine-core";

export const separator: StyleSpec<Record<string, never>> = {
  height: 1,
  marginY: 4,
  marginX: 0,
  background: "#374151",
  variants: {},
};
