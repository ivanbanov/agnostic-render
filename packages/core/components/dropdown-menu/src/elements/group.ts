/**
 * DropdownMenu group — wraps a related set of items under a label.
 * Pure container; carries no cosmetic styling today.
 */

import type { StyleSpec } from "@render-experiment/machine-core";

export const group: StyleSpec<Record<string, never>> = {
  display: "block",
  variants: {},
};
