/**
 * DropdownMenu label — heading text inside a group.
 * Cosmetic-only.
 */

import type { StyleSpec } from "@render-experiment/machine-core";

export const label: StyleSpec<Record<string, never>> = {
  paddingY: 6,
  paddingX: 12,
  fontSize: 11,
  fontWeight: 600,
  color: "#9ca3af",
  textTransform: "uppercase",
  letterSpacing: "0.05em",
  variants: {},
};
