// Agnostic style-spec vocabulary — the shape components use to describe
// per-element styling without committing to a renderer. Target style engines
// (style-engine-react / style-engine-native) translate it to native styles.
export type { Style, StyleSpec, StyleValue } from './spec'
